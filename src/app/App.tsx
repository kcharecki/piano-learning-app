/**
 * Placeholder shell. The real routing and screens land in roadmap task 1.17;
 * this exists so the build, e2e smoke test and dev server work from day one.
 */
export function App() {
  return (
    <main>
      <h1>Piano Learning App</h1>
      <p data-testid="status">Foundation ready — see ROADMAP.md for what lands next.</p>
    </main>
  )
}
