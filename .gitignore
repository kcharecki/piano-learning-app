node_modules/
dist/
coverage/
.vite/
*.tsbuildinfo
.DS_Store
Thumbs.db
playwright-report/
test-results/
.env
.env.local
# Scratch scores dropped in the repo root while testing import — real fixtures
# live in e2e/fixtures/ and src/core/notation/__fixtures__/.
/*.mid
/*.midi
/*.mxl
/*.musicxml
.claude/settings.local.json
# Parallel-session worktrees (docs/WORKTREES.md) and visual-pass screenshots
.claude/worktrees/
visual-pass/
# A second destination's pass, when one slice touches two screens (improve-app 2026-08-24-1).
visual-pass-*/

# /improve-app per-run scratch (docs/commands/improve-app.md §0). Only the per-run
# directories are ignored. runs/ledger.ndjson and runs/orphan-ages.json ARE committed —
# they are the process's only memory between runs, and ignoring them silently resets it.
runs/*/
