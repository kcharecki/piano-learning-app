node_modules/
dist/
coverage/
.vite/
*.tsbuildinfo
.DS_Store
Thumbs.db
playwright-report/
test-results/
.env
.env.local
# Scratch scores dropped in the repo root while testing import — real fixtures
# live in e2e/fixtures/ and src/core/notation/__fixtures__/.
/*.mid
/*.midi
/*.mxl
/*.musicxml
.claude/settings.local.json
# Parallel-session worktrees (docs/WORKTREES.md) and visual-pass screenshots
.claude/worktrees/
visual-pass/

# /improve-app per-run scratch (docs/commands/improve-app.md §0). The append-only
# runs/ledger.ndjson IS committed — it is the process's only memory between runs.
runs/*/
